[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# Standalone scripts and older unittest entry points set up their import path.
"scripts/**/*.py" = ["E402"]
"tests/**/*.py" = ["E402"]
